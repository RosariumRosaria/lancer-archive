{
  "name": "lancer-archive",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "eleventy --serve",
    "build": "eleventy",
    "postbuild": "npx -y pagefind --site _site",
    "search:build": "npm run build && npm run pagefind",
    "pagefind": "npx -y pagefind --site _site",
    "search:serve": "npm run build && npm run pagefind -- --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-navigation": "^1.0.5"
  }
}
